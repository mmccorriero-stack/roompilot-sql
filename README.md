# roompilot-sql
SQL analytics engine for hospitality revenue management. Core queries for KPI, segmentation, forecasting and pricing insights.

## Repository structure

- `kpi/` → core KPI queries
- `booking_behavior/` → lead time, booking window, pickup
- `revenue/` → revenue breakdown by segment and channel
